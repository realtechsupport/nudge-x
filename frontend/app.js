// app.js - thin wrapper to import the JSX-based App component.
// This file intentionally contains no JSX so that bundlers that don't
// treat `.js` as JSX won't choke on angle brackets.

import App from "./App.jsx";
export default App;
